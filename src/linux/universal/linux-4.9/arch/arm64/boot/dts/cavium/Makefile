dtb-$(CONFIG_ARCH_THUNDER) += thunder-88xx.dtb
dtb-$(CONFIG_ARCH_GW6300) += gw6300-linux.dtb
dtb-$(CONFIG_ARCH_GW6304) += gw6304-linux.dtb

always		:= $(dtb-y)
subdir-y	:= $(dts-dirs)
clean-files	:= *.dtb
