
if OS_LINUX

bin_SCRIPTS = sas_disk_blink scsi_ch_swp

endif

if OS_WIN32_MINGW

bin_SCRIPTS = sas_disk_blink

endif

if OS_WIN32_CYGWIN

bin_SCRIPTS = sas_disk_blink

endif

if OS_FREEBSD

bin_SCRIPTS = sas_disk_blink

endif

if OS_SOLARIS

bin_SCRIPTS = sas_disk_blink

endif

if OS_OSF

bin_SCRIPTS = sas_disk_blink

endif
