/* XXX THIS RULE IS NOW DELETED.  MUNGED FAST_PATTERN MATCH, SET TO INITIALIZED, ADDED "DELETED" TO
 * XXX RULE MESSAGE, ADDED return RULE_NOMATCH TO CUSTOM DETECTION FUNCTION.  REPLACED BY PLAINTEXT
 * XXX RULE PER BUG 76599.
 * This code was automatically generated by the Sourcefire VRT Rules2C web generator
 *
 * We make no claims to the correctness or fitness of the code generated or of modifications
 * to the generated code.
 *
 * Use at your own risk.
 *
 * Please report any problems with this tool to research@sourcefire.com
 */

#include "sf_snort_plugin_api.h"
#include "sf_snort_packet.h"


/* declare detection functions */
int rule16663eval(void *p);

/* declare rule data structures */
/* flow:established, to_client; */
static FlowFlags rule16663flow0 = 
{
    FLOW_ESTABLISHED|FLOW_TO_CLIENT
};

static RuleOption rule16663option0 =
{
    OPTION_TYPE_FLOWFLAGS,
    {
        &rule16663flow0
    }
};
#ifndef CONTENT_FAST_PATTERN
#define CONTENT_FAST_PATTERN 0
#endif
// content:"|FF D8|", payload raw, depth 0, fast_pattern; 
static ContentInfo rule16663content1 = 
{
    (uint8_t *) "DELETEDRULEDELETEDRULE3412#$!@#$qEFSFDFASFDELETEDRULEDELETEDRULE|FF D8|", /* pattern (now in snort content format) */
    0, /* depth */
    0, /* offset */
    CONTENT_RELATIVE|CONTENT_FAST_PATTERN|CONTENT_BUF_NORMALIZED, /* flags */
    NULL, /* holder for boyer/moore PTR */
    NULL, /* more holder info - byteform */
    0, /* byteform length */
    0 /* increment length*/
};

static RuleOption rule16663option1 = 
{
    OPTION_TYPE_CONTENT,
    {
        &rule16663content1
    }
};
// content:"|FF C0|", payload raw, depth 100, relative; 
static ContentInfo rule16663content2 = 
{
    (uint8_t *) "|FF C0|", /* pattern (now in snort content format) */
    100, /* depth */
    0, /* offset */
    CONTENT_RELATIVE|CONTENT_BUF_NORMALIZED, /* flags */
    NULL, /* holder for boyer/moore PTR */
    NULL, /* more holder info - byteform */
    0, /* byteform length */
    0 /* increment length*/
};

static RuleOption rule16663option2 = 
{
    OPTION_TYPE_CONTENT,
    {
        &rule16663content2
    }
};
// content:"|FF DA|", payload raw, depth 100, relative; 
static ContentInfo rule16663content3 = 
{
    (uint8_t *) "|FF DA|", /* pattern (now in snort content format) */
    100, /* depth */
    0, /* offset */
    CONTENT_RELATIVE|CONTENT_BUF_NORMALIZED, /* flags */
    NULL, /* holder for boyer/moore PTR */
    NULL, /* more holder info - byteform */
    0, /* byteform length */
    0 /* increment length*/
};

static RuleOption rule16663option3 = 
{
    OPTION_TYPE_CONTENT,
    {
        &rule16663content3
    }
};
/* flowbits:isset "file.avi"; */
static FlowBitsInfo rule16663flowbits4 =
{
    "file.avi",
    FLOWBIT_ISSET,
    0,
};
static RuleOption rule16663option4 =
{
    OPTION_TYPE_FLOWBIT,
    {
        &rule16663flowbits4
    }
};

/* references for sid 16663 */
/* reference: cve "2010-1880"; */
static RuleReference rule16663ref1 = 
{
    "cve", /* type */
    "2010-1880" /* value */
};

/* reference: url "www.microsoft.com/technet/security/bulletin/MS10-033.mspx"; */
static RuleReference rule16663ref2 = 
{
    "url", /* type */
    "www.microsoft.com/technet/security/bulletin/MS10-033.mspx" /* value */
};

static RuleReference *rule16663refs[] =
{
    &rule16663ref1,
    &rule16663ref2,
    NULL
};

/* metadata for sid 16663 */
/* metadata:service http, policy security-ips drop; */
static RuleMetaData rule16663service1 = 
{
    "service http"
};


//static RuleMetaData rule16663policy1 = 
//{
//    "policy security-ips drop"
//};


static RuleMetaData *rule16663metadata[] =
{
    &rule16663service1,
//    &rule16663policy1,
    NULL
};

RuleOption *rule16663options[] =
{
    &rule16663option0,
    &rule16663option1,
    &rule16663option2,
    &rule16663option3,
    &rule16663option4,
    NULL
};

Rule rule16663 = {
   /* rule header, akin to => tcp any any -> any any */
   {
       IPPROTO_TCP, /* proto */
       "$EXTERNAL_NET", /* SRCIP     */
       "$HTTP_PORTS", /* SRCPORT   */
   
       0, /* DIRECTION */
       "$HOME_NET", /* DSTIP     */
   
       "any", /* DSTPORT   */
   },
   /* metadata */
   { 
       3,  /* genid */
       16663, /* sigid */
       5, /* revision */
       "attempted-user", /* classification */
       0,  /* hardcoded priority XXX NOT PROVIDED BY GRAMMAR YET! */
       "DELETED WEB-CLIENT Windows Media Player JPG header record mismatch memory corruption attempt",     /* message */
       rule16663refs /* ptr to references */
       ,rule16663metadata
   },
   rule16663options, /* ptr to rule options */
   &rule16663eval, /* replace with NULL to use the built in detection function */
   1 // DELETED RULE 0 /* am I initialized yet? */
};


/* detection functions */
int rule16663eval(void *p) {
    const uint8_t *cursor_normal = 0;
    const uint8_t *end_of_payload, *cursor_detect;
    SFSnortPacket *sp = (SFSnortPacket *) p;
    uint8_t sofx, sos;

return RULE_NOMATCH; // DELETED RULE

    if(sp == NULL)
        return RULE_NOMATCH;

    if(sp->payload == NULL)
        return RULE_NOMATCH;
    
    // flow:established, to_client;
    if (checkFlow(p, rule16663options[0]->option_u.flowFlags) > 0 ) {

        // flowbits:isset "file.avi";
        if (processFlowbits(p, rule16663options[4]->option_u.flowBit) > 0) {

            if(getBuffer(sp, CONTENT_BUF_NORMALIZED, &cursor_normal, &end_of_payload) <= 0)
                return RULE_NOMATCH;
    
            // content:"|FF D8|", payload raw, depth 0, fast_pattern;
            while (contentMatch(p, rule16663options[1]->option_u.content, &cursor_normal) > 0) {
    
                cursor_detect = cursor_normal;
    
                // content:"|FF C0|", payload raw, depth 100, relative;
                if (contentMatch(p, rule16663options[2]->option_u.content, &cursor_detect) > 0) {
    
                    if ((cursor_detect + 8) > end_of_payload) {
                        return RULE_NOMATCH;
                    }
                    cursor_detect += 7;
                    sofx = *cursor_detect++;
                    // content:"|FF DA|", payload raw, depth 100, relative;
                    if (contentMatch(p, rule16663options[3]->option_u.content, &cursor_detect) > 0) {
                        if ((cursor_detect + 3) > end_of_payload) {
                           return RULE_NOMATCH;
                        }
                        cursor_detect += 2;
                        sos = *cursor_detect;
                        if (sos != sofx) {
                            return RULE_MATCH;
                        }
                    }
                }
            }
        }
    }
    return RULE_NOMATCH;
}

/*
Rule *rules[] = {
    &rule16663,
    NULL
};
*/

