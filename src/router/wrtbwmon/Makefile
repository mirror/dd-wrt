clean:
	@true
all:
	@true

install:
	install -d $(INSTALLDIR)/usr/share/wrtbwmon
	install -d $(INSTALLDIR)/usr/sbin
	cp wrtbwmon wrtbwmon.ipv6 readDB.awk readDB_ipv6.awk $(INSTALLDIR)/usr/sbin
	cp usage.htm1 usage.htm2 $(INSTALLDIR)/usr/share/wrtbwmon

