#!/bin/sh
[[ ! -z $(nvram get wg_debug) ]] && { DEBUG=1; set -x; }
SLEEPTIME=$(nvram get oet_sleeptime)
: "${SLEEPTIME:=0}"
sleep $SLEEPTIME
# run asynchrounous of firewall
/usr/bin/eop-tunnel-prewall.sh &
#( nohup /usr/bin/eop-tunnel-prewall.sh & ) >/dev/null
